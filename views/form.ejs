<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
  </head>
  <body>
    <section class="px-10 py-5 bg-slate-50 h-full">
      <!---------- header start ------------>
      <%- include('header') %>
      <!------------ header end ------------>

      <div class="form-section mt-5">
        <!----------- form ----------->
        <form
          method="post"
          action="<%= editData ? '/employee/edit?id=' + editData.id : '/form' %>"
        >
          <div
            class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-2 md:gap-3 lg:gap-6 mb-2 md:mb-3 lg:mb-6"
          >
            <!------------ personal information ----------->
            <div class="form-item">
              <div
                class="bg-white shadow-lg hover:shadow-xl transition-all duration-300 p-6 rounded-3xl h-full"
              >
                <h2
                  class="text-xl font-semibold mb-4 text-indigo-600 border-b pb-2"
                >
                  Personal Information
                </h2>
                <div class="space-y-4">
                  <!-- full name -->
                  <div>
                    <label
                      htmlFor="employeeName"
                      class="block mb-1 text-sm font-medium"
                    >
                      Full Name
                    </label>
                    <input
                      type="text"
                      id="employeeName"
                      name="employeeName"
                      value="<%= editData ? editData.employeeName : '' %>"
                      class="w-full bg-gray-50 border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-blue-500 focus:outline-none"
                      placeholder="John Doe"
                    />
                  </div>

                  <!-- employee email -->
                  <div>
                    <label
                      htmlFor="email"
                      class="block mb-1 text-sm font-medium"
                    >
                      Email
                    </label>
                    <input
                      type="email"
                      id="email"
                      name="email"
                      value="<%= editData ? editData.email : '' %>"
                      class="w-full bg-gray-50 border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-blue-500 focus:outline-none"
                      placeholder="JohnDoe@gmail.com"
                    />
                  </div>

                  <!-- department -->
                  <div>
                    <label
                      htmlFor="department"
                      class="block mb-1 text-sm font-medium"
                    >
                      Department
                    </label>
                    <select
                      id="department"
                      name="department"
                      value="<%= editData ? editData.department : '' %>"
                      class="w-full bg-gray-50 border-gray-300 border rounded-lg px-4 py-2 focus:ring-2 focus:ring-blue-500 focus:outline-none"
                    >
                      <option value="">Select Department</option>
                      <option value="hr">Human Resources</option>
                      <option value="tech">Technology</option>
                      <option value="finance">Finance</option>
                      <option value="marketing">Marketing</option>
                      <option value="operations">Operations</option>
                    </select>
                  </div>

                  <!-- employee basic salary -->
                  <div class="space-y-4">
                    <div>
                      <label
                        htmlFor="salary"
                        class="block mb-1 text-sm font-medium"
                      >
                        Basic Salary
                      </label>
                      <div class="relative">
                        <div
                          class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none text-gray-400"
                        >
                          ₹
                        </div>
                        <input
                          type="number"
                          min="15000"
                          max="75000"
                          id="salary"
                          name="employeeSalary"
                          value="<%= editData ? editData.employeeSalary : '' %>"
                          class="w-full bg-gray-50 border-gray-300 border rounded-lg px-4 py-2 pl-8 focus:ring-2 focus:ring-blue-500 focus:outline-none"
                          placeholder="0.0"
                        />
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>

            <!-------------- services timing --------------->

            <div class="form-item">
              <div
                class="bg-white shadow-lg hover:shadow-xl transition-all duration-300 rounded-3xl h-full p-6"
              >
                <h2
                  class="text-xl font-semibold mb-4 text-indigo-600 border-b pb-2"
                >
                  Service Timings
                </h2>
                <div>
                  <label
                    htmlFor="shiftStart"
                    class="block text-sm font-medium mb-1"
                  >
                    Shift Start Time
                  </label>
                  <input
                    type="time"
                    id="shiftStart"
                    name="shiftStart"
                    value="<%= editData ? editData.shiftStart : '' %>"
                    class="w-full mb-5 bg-gray-50 border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-blue-500 focus:outline-none"
                  />
                </div>
                <div>
                  <label
                    htmlFor="shiftEnd"
                    class="block text-sm font-medium mb-1"
                  >
                    Shift End Time
                  </label>
                  <input
                    type="time"
                    id="shiftEnd"
                    name="shiftEnd"
                    value="<%= editData ? editData.shiftEnd : '' %>"
                    class="w-full bg-gray-50 border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-blue-500 focus:outline-none"
                  />
                </div>
              </div>
            </div>

            <!------------ employee descriptions ------------>
            <div class="form-item md:col-span-2 lg:col-span-2">
              <div
                class="bg-white shadow-lg hover:shadow-xl transition-all duration-300 rounded-3xl p-6"
              >
                <h2
                  class="text-xl font-semibold mb-4 text-indigo-600 border-b pb-2"
                >
                  Employee Description
                </h2>
                <div>
                  <label
                    htmlFor="description"
                    class="block text-sm font-medium text-gray-700 mb-1"
                  >
                    Professional Summary
                  </label>
                  <textarea
                    id="description"
                    rows="{5}"
                    class="w-full bg-gray-50 border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-blue-500 focus:outline-none"
                    placeholder="Describe the employee's role, skills, and responsibilities..."
                    name="description"
                    value=""
                  >
                    <%= editData ? editData.description : '' %>
                    </textarea
                  >
                </div>
                <!-- key skills -->
                <div class="mt-4">
                  <label
                    htmlFor="skills"
                    class="block text-sm font-medium text-gray-700 mb-1"
                  >
                    Key Skills
                  </label>
                  <input
                    type="text"
                    id="skills"
                    class="w-full bg-gray-50 border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-blue-500 focus:outline-none"
                    placeholder="Add skills separated by commas"
                    name="skills"
                    value="<%= editData ? editData.skills : '' %>"
                  />
                </div>

                <!-- additional notes on employees -->
                <div class="mt-4">
                  <label
                    htmlFor="notes"
                    class="block text-sm font-medium text-gray-700 mb-1"
                  >
                    Additional Notes
                  </label>
                  <textarea
                    id="notes"
                    rows="{3}"
                    class="w-full bg-gray-50 border border-gray-300 rounded-lg px-4 py-2 focus:ring-2 focus:ring-blue-500 focus:outline-none"
                    placeholder="Any other relevant information..."
                    name="notes"
                    value=""
                  >
                    <%= editData ? editData.notes : '' %>
                    </textarea
                  >
                </div>
              </div>
            </div>
          </div>

          <button
            class="bg-gradient-to-br from-indigo-500 to-purple-600 transition-colors hover:scale-107 duration-300 text-white py-2 px-7 mt-5 rounded-lg cursor-pointer"
          >
            Submit
          </button>
        </form>
      </div>
    </section>
  </body>
</html>
